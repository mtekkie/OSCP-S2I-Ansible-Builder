---
- hosts: localhost
  vars:
    hello: world
  tasks:
  # - name: install the latest version of Apache
  #   yum:
  #     name: httpd
  #     state: latest

  - name: install the latest version of Find
    yum:
      name: findutils
      state: latest
